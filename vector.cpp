#include <iostream>
#include <string>
#include <vector>

using namespace std;

void invec(vector<int> vec){
    for(int i = 0; i < vec.size(); i++){
        cout << vec.at(i) << endl;
    }
}

int main(int argc, char *argv[]){
    vector<int> vec;
    // vec.resize(10, 0);
    // vec[8] = 1;
    // vec[2] = 9;
    // vec.push_back(1);
    // vec.push_back(2);
    
    for(int i = 0; i < ; i++)
    invec(vec);
    
    return 0;
}